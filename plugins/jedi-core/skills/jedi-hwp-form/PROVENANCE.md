# 근거 — jedi-hwp-form

> 이 문서는 **출처·실측 기록 전용**이다. 동작을 정하는 값은 여기 두지 않는다.
> 작성: 2026-08-08 / 작성자: 줄갭

## 어디서 나온 것인가

2026-08-08 협회 제출용 신청서(6쪽, 표 65칸·사진·도장) + 동의서(1쪽, 도장)를
공양식에서 스크래치로 작성하며 얻은 처방이다. 한 세션에서 **8번 재빌드**했고
그 중 4번은 진단이 틀려서였다. 함정 9종은 전부 그 과정에서 하나씩 드러난 것이다.

최종 산출물은 기존 방식(한글 COM 직접 조작)으로 만든 완성본과 대조해 검수했다:

| 항목 | COM 방식 | 이 방식 |
|---|---|---|
| 쪽수 | 6 | 6 |
| 그림 3개 크기·위치 | — | 전부 일치 (0.4mm 이내) |
| 본문 2~6쪽 글자 | 3,892자 | **3,892자 · 공백무시 동일** |
| 여백 | 좌10.0 상9.3 우끝183.7 | 동일 |
| 동의서 | 1쪽 / 491자 | **1쪽 / 491자 · 완전 일치** |

## 핵심 수치

- `1mm = 283.46 HWPUNIT` — 도장 `4275` 를 15.08mm 로 역산해 검증
- 그림 표시 크기 = **이미지 픽셀 / 96dpi** — 152px 도장에 15.1mm 를 지정했으나
  40.3mm 로 그려졌고 `152 / 96 * 25.4 = 40.2mm` 와 일치
- 줄배치를 비우면 재계산 — 179자가 1줄 → **24줄**로 접힘

## 실제 지원사업 양식 15종 검증 (2026-08-08)

보관 중인 지원사업 서류 182개 중 성격이 다른 15종을 골라 **끝까지** 돌렸다
(`.hwp` → `.hwpx` → 구조 파싱 → 빈칸에 시험값 → `.hwp`/`.pdf` → 결과 확인).

| 양식 | 쪽 | 칸 | 빈칸 | 결과 |
|---|---|---|---|---|
| 지급요청서 | 1 | 30 | 9 | ✅ |
| 표준용역계약서 | 5 | 24 | 9 | ✅ |
| 검수확인서 | 1 | 46 | 28 | ✅ |
| 중간보고서 | 2 | 97 | 36 | ✅ |
| 과업지시서 | 3 | 61 | 41 | ✅ |
| 사업비변경보고서 | 3 | 214 | 141 | ✅ |
| 기자재임차계약서 | 1 | 25 | 9 | ✅ |
| 사업신청서(도) | 14 | 479 | 144 | ✅ |
| 사업신청서(날인완료) | 32 | — | — | ✅ |
| 등록신청서 | 1 | 17 | 8 | ✅ |
| 개인정보 동의서 | 1 | 20 | 6 | ✅ |
| 솔루션 설명자료 | 2 | 36 | 7 | ✅ |
| 수행계획서 | **33** | **1,549** | **807** | ✅ |
| 수요-공급 매칭신청서 | 3 | 42 | 18 | ✅ |
| 공고문(`.hwpx` 원본) | 11 | 198 | 90 | ⚠️ 원본 자체의 PDF 변환이 불안정(11쪽 → 6쪽, MuPDF 구문오류). 편집과 무관하며 공고문은 채울 문서가 아님 |

**1,549칸 33쪽짜리 대형 양식도 통과**했다. 표·글자·쪽수·그림 수가 보존됐다.

⚠️ 1차 시도에서 4종이 «값이 안 보임»으로 나왔는데, 원인은 라이브러리가 아니라
**검증 스크립트가 빈칸 목록의 첫 칸을 무조건 골랐기 때문**이었다. 그 자리
(대개 `(r0,c1)`)가 화면에 안 나오는 칸이었고, **다음 칸에 넣으니 전부 보였다**.
→ 채울 칸은 사람이 확인하고 고를 것. 「빈칸이니 아무 데나」는 안 된다.

## 팝업 2종 (실측 처방)

작업 중 뜨는 창이 두 가지다. 성격이 완전히 다르다.

| 창 | 원인 | 처방 | 효과 |
|---|---|---|---|
| **보안 승인** («접근 허용») | 검사 모듈 DLL 은 설치돼 있는데 레지스트리 미등록 | `HKCU\Software\HNC\HwpAutomation\Modules` 에 등록 + `RegisterModule` 두 번째 인자를 **그 키 이름과 동일하게** | 파일 여는 시간 **42.5초 → 0.1초** |
| **버전 경고** («상위 버전에서 작성한 문서입니다») | 설치된 한글(11.0)보다 최신 버전으로 만든 문서 | `SetMessageBoxMode` 로는 안 막힌다 → 확인창 자동 닫기 감시 | 한글을 올리면 근본 해소 |

🔴 **`RegisterModule` 의 두 번째 인자가 레지스트리 키 이름과 다르면 `False` 를 돌려주고
조용히 팝업 대기로 되돌아간다.** 반환값을 안 보면 모른다. 이 이름 불일치가 42.5초의 정체였다.

부수 효과: 팝업 대기 중 타임아웃으로 `com_error` 가 나던 파일(32쪽 날인완료본)이 정상 처리됐다.

## 막힌 길 (다시 시도하지 말 것)

**`.hwp` 직접 쓰기.** `hwp-mcp`(PyPI `hwp-mcp` 0.1.1, MIT, Treesoop)는 OLE 컨테이너를
재구성해 `.hwp` 를 쓴다. 실측 결과:

| 파일 | 자체 파서로 재읽기 | **한글로 열기** |
|---|---|---|
| 동의서 | ✅ 460자, 치환 반영 | ❌ `Open=False`, 본문 0자 |
| 신청서 양식 | — | ❌ `Open=False`, 본문 0자 |
| 같은 라이브러리의 `.hwpx` 경로 | — | ✅ 2쪽 / 1,137자 |

자기 검증은 통과하는데 **진짜 소비자(한글)가 거부**하는 형태다. `.hwpx`(zip 재작성)만
작동하므로 편집은 반드시 그 경로로 한다.

## `hwp-mcp` 와의 역할 분담

같은 파일을 다루지만 겹치지 않는다.

| 단계 | 담당 | 이유 |
|---|---|---|
| `.hwp` 읽기·표·그림 목록 | **`hwp-mcp`** | 한컴 없이 읽는다. 이 스킬에 없는 능력 |
| `.hwpx` 편집 | **`hwpx_lib`** | `hwp-mcp` 의 쓰기는 `content.replace()` 한 줄이라 함정 9종이 안 걸러진다 |
| 변환·검수 | **`hwpform.py`** | 한컴 COM + PDF 대조 |

`hwp-mcp` 는 `jedi-core/.mcp.json` 에 이미 등록돼 있어 별도 배선이 필요 없다.

## 검증 안 된 것 (약속하지 않는다)

- **백지에서 새 문서 만들기** — 서식 정의(글꼴·문단·테두리)를 통째로 지어내야 한다.
  근거가 없으므로 이 스킬의 범위에서 뺐다.
- **표 구조 자체를 바꾸기** (행 추가·칸 병합) — 값 채우기만 검증했다.
- **`.hwpx` 왕복 무손실** — `.hwp → .hwpx → .hwp` 왕복은 쪽수·그림·글자 일치를
  확인했으나 표본이 1건이다.

## 짝 문서

- 함정 상세: 개인 메모리 `reference_hwpx_xml_editing_traps`
- 한글 COM 직접 조작 방식의 함정: 개인 메모리 `reference_hwp_com_setpos_list_is_cell_plus_4`
