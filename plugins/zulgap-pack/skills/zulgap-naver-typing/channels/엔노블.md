# 엔노블 — 네이버 타이핑 설정

| 항목 | 값 | 설명 |
|---|---|---|
| 프로필키 | `n-noble` | **전용 계정.** ff1030(마미사·줄갭·RPG)·gdgodental 과 다른 창을 쓴다 |
| 블로그ID | `n-noble` | https://blog.naver.com/n-noble |
| 동료사명 | `엔노블` | 노션 실측 2026-08-16 — 네이버블로그 카드 216건 (타이핑 요청 4) |
| 카테고리번호 | `8` | 스킬이 이 번호로 찾는다 |
| 카테고리 | `Noble_love` | 번호가 맞는지 **대조**하는 값 |

> 🔑 **사장님 확정 (2026-08-16)** — 밀린 4편(결혼정보회사 등급표 · 의사 결혼 · 재혼 · 중매결혼)은
> 전부 `Noble_love`. 글쓰기 창 기본값은 `11 N.NOBLE_의문` 이라 **그냥 두면 다른 자리로 올라간다.**

> 🔴 **번호가 이름 순서와 무관하다.** 1 → 10 → 12 → 11 → 13 → 6 → 7 → 8 → 9 순으로 나온다.
> 눈으로 세지 말고 이 파일의 번호를 쓸 것.

## 카드 구조가 다르다 (파서가 이미 안다)

- 카드에 **`# 제목` 줄이 없다** — 노션 「기획안(👇클릭해주세요)」 값이 제목이다.
  `validateCard(parsed, { title })` 로 넘기고 `parsed.title` 에도 넣어 준다.
- 본문에 **이모지 인용문 메모**(`> 📌` `> 🔁` `> ✅` `> ⚠️` `> 🗂️`)와
  **AI 이미지 지시줄**(`🖼️ **이미지1 (AI)**:`) + 프롬프트 코드블록이 섞여 있다. 파서가 자른다.
- 픽스처는 `_test-card-parser.js` [8] 절에 있다.

## ⚠️ 썸네일이 카드에 없다 (2026-08-16 확인 — 미해결)

블로그는 **전부 썸네일을 넣는 것이 방침**인데(사장님 확정), 엔노블 카드에는 썸네일 절이 없다.
그래서 지금 태우면 검색결과 얼굴이 **본문 첫 그림**이 된다.
→ **카드를 만드는 쪽을 고치는 것으로 결정**(2026-08-16). 그 전까지 타이핑은 보류.

## 처음 한 번 (이미 해둠 — 2026-08-16)

```bash
node scripts/chrome-session.js open n-noble "https://blog.naver.com/n-noble?Redirect=Write"
```

사장님이 이 창에서 직접 로그인 + 「로그인 상태 유지」 켬.
로그아웃되면 같은 명령으로 다시 띄우고 **사람이** 로그인한다 — 비밀번호는 도구가 다루지 않는다.

## 메모

- 담당자: 사장님
- 특이사항: 카테고리 9개. `N.NOBLE_*` 5개와 `Noble_*` 4개 두 계열이 섞여 있다.
