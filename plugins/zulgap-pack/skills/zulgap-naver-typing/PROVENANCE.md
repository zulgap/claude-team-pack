# 근거·실측 이력

> 이 스킬의 모든 판정은 2026-08-15~16 **실행 기반 실측**에서 나왔다. 추측으로 쓴 값이 없다.
> 설계 정본: `~/.claude/specs/2026-08-15-naver-blog-typing-automation.md`

## 왜 만들었나 — 실측

노션 콘텐츠마스터 「네이버블로그」 카드 전수 (2026-08-15)

| 단계 | 건수 |
|---|---:|
| 13. 발행보고완료 | 846 |
| 11. 발행완료 | 228 |
| **8. 타이핑 요청** | **26** ← 사람 대기 |
| 9. 타이핑 완료 | 9 |

- **누적 1,074편을 전부 손으로 타이핑**했다
- 처리 속도: 최근 60일 **28편 = 하루 0.47편** (처리한 날 18일 / 42일은 0편)
- 밀린 26편 소화에 **약 56일** ↔ 발행 예정일은 2주 내 → **구조적 미달**
- 편당 이미지 6~7장 → 26편이면 **약 180장**

## 네이버 쪽 사실

| 사실 | 근거 |
|---|---|
| 발행 API가 없다 | 네이버 「글쓰기 API」 2020-05-06 종료 (사유: 광고성 글 대량 게재) |
| **외부 이미지 URL 을 안 가져간다** | 붙여넣기 직후·**임시저장 후에도** 외부 주소 유지 (2026-08-15 실측) |
| 발행 요청에 캡차 토큰이 필요하다 | `RabbitWrite.naver` + ncaptcha `tokenId` — **공개 발행 때만** |
| 매크로는 명시적 제재 대상 | 네이버 운영정책 |

→ **등록은 사람이 누른다.** 네이버가 방어선을 세운 지점을 사람이 통과한다.

## 에디터 구조 (실측으로 확정 — 추측 금지)

| 무엇 | 값 | 함정 |
|---|---|---|
| 에디터 프레임 | `name === 'mainFrame'` | 루트에는 없다 |
| 본문 컨테이너 | **`.se-content`** | 🔴 `.se-main-container` 는 **존재하지 않는다**. 이걸로 읽어 «성공한 주입을 0자 실패로» 2번 오독했다 |
| 문단 | `.se-text-paragraph` | 클릭 후 `End` 로 끝으로 보내야 한다 (안 하면 문단 중간에 박힌다) |
| `contenteditable` | 1개인데 **클릭 불가** | 숨은 input buffer |
| file input | **0개** | 「사진」 버튼 클릭 → `filechooser` 가로채기 |
| 빈 창 판정 | `.se-placeholder` 의 `display` | 🔴 글자수로 판정 금지 — 빈 창도 55자(캠페인 문구, **수시로 바뀜**). `cloneNode`+`innerText` 도 안 된다(숨은 텍스트까지 세서 88자로 **늘어남**) |
| 시작 팝업 | 「작성 중인 글이 있습니다」 | 🔴 «확인»을 누르면 **이전 원고를 불러와 섞인다**. 반드시 «취소» |

## 키보드 타이핑 vs 붙여넣기 (사장님 질문 → 실측으로 결정)

```
p.keyboard.type('한글타이핑시험입니다', {delay:30})
→ 정확히 입력됨 ✅ · 471ms (글자당 47ms)
→ 🔴 문단 «한가운데»에 박혔다: "여기까[한글타이핑시험입니다]지 복사"
```

| 축 | 붙여넣기 | 키보드 타이핑 |
|---|---|---|
| 한글 | ✅ | ✅ (IME 문제 없음) |
| 2,000자 | 즉시 | 약 94초 |
| **소제목·굵게·표** | ✅ | 🔴 **불가** — 평문. 서식은 드래그+툴바라 훨씬 잘 깨진다 |
| 커서 | 무관 | 🔴 매번 정밀 제어 |

**결론: 붙여넣기.** 단 **클립보드는 쓰지 않는다**(팀원이 작업 중 복사하면 깨지므로) —
`DataTransfer` 로 paste 이벤트를 에디터에 직접 전달한다. 실측에서 서식 유지된 채 주입 성공.

## 위험에 대해 «증명하지 못한» 것

확인된 것은 **「에디터 주입이 된다」**까지다. **「계정 위험이 낮아진다」는 증명되지 않았다.**

| 확인됨 | 미확인 |
|---|---|
| 자동화 표식(`navigator.webdriver`) 미노출 | 네이버가 **다른 축**으로 탐지하는지 |
| 평소 크롬·IP·쿠키 사용 | 그것으로 **충분한지** |

🔴 감지는 확률적이다(*"수백 개를 똑같이 했는데 몇 개만 걸렸다"* — 운영 경험담).
**무사통과가 안전의 증거가 못 된다.** 그래서 위험을 0으로 만들었다고 주장하지 않고
**줄인다**고만 말한다. **초기 2주는 하루 5편 이하**, 이상 신호 시 즉시 중단.

## e2e 검증 기록

**에디터 채우기** (실제 네이버 글쓰기 창 · 2회 연속)
```
status: ready_to_register · problems: []
본문 400자 · 이미지 2/2 네이버 서버 · 외부 URL 0장 · 내부 메모 0건 · 12.8초
화면 확인: 사진 제자리, 소제목·목록·해시태그 서식 유지
```

**노션 선점·완료** (실제 카드 1편 · 2026-08-16)
```
8. 타이핑 요청 → 8-1. 타이핑 중   「8. 타이핑 요청」 26 → 25 (다른 사람 조회에서 제외) ✅
8-1 → 9. 타이핑 완료 + 완료일자 2026-08-16 ✅
원상복구 → 26편 / 8-1 0건 / 완료일자 0건 (흔적 없음) ✅
```

**단위 시나리오** — 전부 2회 연속 PASS
```
chrome-session  13/13     card-parser  22/22     typing-state  17/17
```

## 노션 스키마 변경 이력

**2026-08-16** — 「단계」 select 에 `8-1. 타이핑 중`(orange) 추가 (23 → 24 옵션)
- 기존 23 옵션 + 색상 전부 보존 확인
- 데이터 무손상: 발행보고완료 846 / 발행완료 228 / 타이핑요청 26 — 변화 0
- ⚠️ 부작용: 페이지 템플릿 이름이 `"기사작성 템플릿"` → `"Untitled"` 로 바뀜 (노션에서 복구 필요)

**왜 이 방법인가**: 「타이핑 담당자」(person) 로 선점하려 했으나, 실측 결과
「8. 타이핑 요청」 26편 중 **3편에 이미 담당자가 배정**돼 있어 「작업 중」과 구분되지 않았다.
그래서 「단계」 축을 쓴다 — 새 속성 0개이고 노션 목록에서 사람 눈에 바로 보인다.
