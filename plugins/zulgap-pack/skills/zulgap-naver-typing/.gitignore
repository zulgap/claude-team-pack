# 🔴 절대 커밋 금지 — 크롬 프로필에 네이버 로그인 쿠키(NID_AUT/NID_SES)가 들어 있다.
#    커밋하면 그 계정이 레포를 보는 모든 사람에게 넘어간다. 팀팩 레포는 조직 공개다.
.profiles/

# 로컬 의존 (팀원이 각자 npm install --prefix 로 받는다)
node_modules/

# 작업 중 산출물
tmp-img/
.outbox.jsonl
*.local.json
