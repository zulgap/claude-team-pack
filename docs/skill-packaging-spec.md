# 스킬 패키징 프로토콜 (Skill Packaging Spec) v1

> **정본(SSOT)** — 팀팩/마켓에 배포되는 모든 **공유(shared) 스킬**의 규격.
> 목적: 좋은 스킬 1개를 여러 회사가 "설치하면 자기 회사에 맞게 조정된 상태"로 쓸 수 있게 한다.
> 근거 spec: `~/.claude/specs/2026-07-14-teampack-productization-skill-packaging.md` (JEDI Business OS 1단계)
> 짝 게이트: jedi-forge SKILL.md Phase 0-D (5) + Phase 2 매핑 체크리스트

## §0 적용 범위

| 대상 | 적용 |
|---|---|
| `tier: shared` 스킬 (마켓/타 테넌트 배포 후보) | **전체 필수** (§1~§4) |
| `tier: tenant-only` 스킬 (특정 회사 전용 — 예: 검단가온보고서, 노블냥) | §5만 (전용 팩 격리). §1~§4 면제 |
| 백엔드 런타임 스킬 (`unified-agent/skills/`, agent_skill DB) | **해당 없음** — 층이 다름. 그쪽 정본 = `jedi-forge/references/skill-spec.md` |

## §1 3층 구조 (파일 레이아웃)

```
skills/<스킬명>/
├── SKILL.md          ← 본체 (모든 회사 공용 알고리즘·지시문)
├── channels/         ← 치수 카드 (회사·채널당 1파일 — 테넌트 값은 전부 여기)
│                        ⚠️ 옛 이름 `presets/`는 2026-07-30 v2.20에서 개명됐다. 실물 0개 — 새로 만들지 말 것
│   └── <회사·채널>.md
├── templates/ scripts/ ← 공유 부품 (선택)
└── PROVENANCE.md     ← 근거·출처·실측 이력 (선택 — 심사 grep 제외 영역)
```

### 🔴 하드리밋 — 고객사 정보 4등급 (2026-07-30 확정)

| 등급 | 무엇 | `shared` | `tenant-only` |
|---|---|---|---|
| **A 절대금지** | 테넌트 UUID · 노션 page/DB ID(32-hex) · 공유폴더 UNC 경로(`\\…`) · 개인 PC 절대경로(`C:\Users\…`) · 토큰·키 | ❌ **0건** | ⚠️ 허용하되 **그 회사에만 활성화**가 전제 |
| **B 본체금지** | 회사·브랜드·채널명 · 담당자명 · 브랜드 색상 지정 · 특정 회사 인물/운영 규칙 | ❌ **0건** → 값 파일로 | ✅ 허용 |
| **C 위치제한** | 자사(줄갭)명 · 근거·출처·실측 이력 | `PROVENANCE.md`에만 | 동일 |
| **D 무해** | 기술 용어 · 도구 이름 · 공개 URL | ✅ | ✅ |

**위반 = 심사 REJECT.** 판정은 사람 눈이 아니라 **`check-plugin-consistency.js` Tier D**가 한다(§4⑧).

> 🔴 **A급이 특별한 이유**: B급은 새어도 "누구 것인지 보이는" 수준이지만, A급은 **남의 회사 폴더에 파일이 써지거나 남의 계정으로 유료 API가 호출되는** 경로다. 그래서 `shared`에서는 예외가 없다.
> ⚠️ **`tenant-only`의 A급을 지우면 안 되는 경우가 있다** — 테넌트 가드(`image_url`에 자사 tenant UUID가 있는지 확인하는 류)는 **격리 장치 자체가 A급**이다. 해법은 리터럴 제거가 아니라 **그 팩을 해당 회사에만 활성화**하는 것(§5).
- 회사별 **값** → `channels/<회사·채널>.md`
- 회사별 **인프라 설정**(노션 허브 ID 등) → 하드코딩 금지, `GET /mcp/ext/teampack-config`(토큰 tenant 기준) 소비
- 근거·출처·실측 이력(정본 링크·측정 표본 등) → `PROVENANCE.md` (심사 grep **제외** — 단, 여기에 동작 분기를 넣는 것은 금지. 출처 기록 전용)
- `templates/` 주석의 실측 출처 표기는 허용 (동작 분기 금지 — 동작을 정하는 값은 preset이 인자로 주입)

### 🔴 실행 이식성 — OS 종속 경로 · 번들 자산 라이선스 (2026-08-03 신설)

위 4등급표는 **정보 노출**의 축이다. 그런데 2026-08-03 실측에서 **정보가 아니라 실행이 깨지는** 별개 클래스가 드러났다 — 아래 3건은 CI(Tier A~E)·`node --check`·문서 리뷰를 **전부 통과한 뒤 e2e 실행에서야** 나왔다.

| 무엇 | 실제로 벌어진 일 |
|---|---|
| `C:/Windows/Fonts/malgunbd.ttf` 직접 복사 | macOS/Linux 에서 그 줄에서 즉사 (2스킬 3파일) |
| 폰트 759KB 번들 + 라이선스 사본 0건 | 레포가 **public** 이라 커밋 자체가 재배포 — OFL 조건 미충족 |
| `npm install` 을 `cwd` 만 지정 | 스킬 폴더에 `package.json` 이 없으면 **상위로 올라가 남의 프로젝트에 설치** → 자기 폴더 require 실패 = 팀원 첫 실행 100% 실패 |

🔴 **4등급표로는 안 잡힌다.** A급 '개인 PC 절대경로'는 `C:\Users\` 패턴이고 `C:/Windows/` 는 개인 경로가 아니다. **`tenant-only` 면제 대상도 아니다** — 정보 노출이 아니라 그냥 안 돌아가는 문제라, 그 회사 안에서도 맥 쓰는 사람은 못 쓴다.

**규칙**

- **OS 종속 절대경로는 단독으로 두지 않는다.** 후보 배열로 두고 존재하는 첫 경로를 고르며, 하나도 없으면 **안내와 함께 실패**시킨다(조용히 넘어가면 깨진 결과물이 납품된다). 대체본이 선택되면 경고를 남긴다. 참조 구현: `zulgap-noblenyang/scripts/resolve-font.js`
  - 의도적으로 그 OS 전용이면 주석에 `@AI:ALLOW os-specific-path`
- **재배포 불가 폰트(Times New Roman·맑은 고딕 등)는 번들하지 않는다** — 시스템에서 탐색한다
- **번들하는 폰트에는 라이선스 사본을 같은 폴더에 둔다.** 판정은 추측하지 말고 폰트 메타데이터 대조: `TTFont(f)['name'].getDebugName(0)`(저작권) / `getDebugName(14)`(라이선스 URL) → 배포처 원문과 저작권 표기가 일치하는지 확인 후 그 원문을 넣는다
- **패키지 설치는 대상 폴더를 명시한다** — `npm install <pkg> --prefix "<dir>"`
- **경로를 파일에 적어 다른 프로그램에 넘길 때는 절대경로로 적는다** — 해석 기준이 cwd 가 아닐 수 있다(ffmpeg concat 목록은 **목록 파일 위치** 기준. 출력을 절대경로로 주면 우연히 통과해 상대경로 실행에서만 재현된다)

**판정은 `check-plugin-consistency.js` Tier F**가 한다. A~E 가 `SKILL.md` 만 보는 것과 달리 **`scripts/` 안 소스를 본다** — 팀원 PC에서 실제로 죽는 코드가 거기 있기 때문이다.

> ⚠️ 판정 기준은 "Windows 경로를 썼나"가 아니라 **"대안을 갖췄나"** 다. 전자는 정당한 후보 배열까지 FAIL 시켜 정답을 막는다.

## §2 빈칸 선언 (frontmatter)

본체 SKILL.md frontmatter에 "나를 회사에 맞추려면 무엇이 필요한가"를 선언한다:

```yaml
---
name: <스킬명>
description: <표준 description — 회사명 예시 금지, "<채널명>" 플레이스홀더 사용>
tier: shared                    # shared | tenant-only
preset_slots:                   # 프리셋 빈칸 (치수 카드 양식)
  타깃페르소나: { required: true,  source: interview }
  강조색:       { required: false, source: company_memory.brand.color }
  금칙어:       { required: false, source: interview }
requires:                       # 필요한 연결 (설치 시 충족 확인)
  jedi_tools: [ext_generate_image]          # 제디 도구
  endpoints: ["/mcp/ext/render-thumbnail"]  # 백엔드 endpoint
  config: []                                # teampack-config 계약 키 (예: notion.master_hub_id)
---
```

- `source` enum: `company_memory.*`(3DB에서 자동 채움 시도) / `tenant_config.*`(회사 카드에서 자동) / `interview`(설치 시 질문)
- `required: true` 슬롯이 미충족이면 스킬은 **명시 안내 후 범용 기본값** 또는 중단 — silent 진행 금지
- 슬롯 이름은 프리셋 파일의 섹션과 1:1 대응 (설치 인터뷰가 기계적으로 채울 수 있게)

## §3 설치 인터뷰 규약 (설치측 Claude 행동 규칙)

새 회사에 shared 스킬을 설치할 때, 설치를 진행하는 Claude는:

1. **`preset_slots` 읽기** — 빈칸 목록 파악
2. **자동 채움 시도** — `source`가 `company_memory.*`/`tenant_config.*`인 슬롯은 회사 메모리(3DB)·회사 카드에서 먼저 조회
3. **미충족 required만 질문** — 이미 채워진 슬롯 재질문 금지. 질문은 슬롯당 1개, 예시 포함
4. **`channels/<회사·채널>.md` 생성** — 동봉 예시 값 파일의 섹션 구조를 그대로 따라 작성
5. **`requires` 충족 확인** — 제디 도구/endpoint 미가용이면 **명시 안내** (silent 진행 금지)

## §4 마켓 등록 심사 항목 (품질 게이트)

> 🔴 제목에 **개수를 쓰지 않는다** — 항목이 늘 때마다 낡아 그 자체가 거짓 문장이 된다.
> 실제로 "9항목"이 Tier F·G·H 신설 뒤에도 갱신되지 않아 오래 거짓이었다(2026-08-05 발견).

| # | 항목 | 검증 방법 |
|---|---|---|
| ① | 본체 테넌트 리터럴 0 | §1 **4등급** 기준. `shared`는 A·B급 0건 (PROVENANCE.md·값 파일 제외). 자동 검사 = 아래 ⑧ |
| ② | preset_slots 선언 완비 | frontmatter에 `tier` + `preset_slots` 존재, required 슬롯 ≥1. `version` + `origin`(personal/teampack)도 필수 (mtime 추측 제거 — 2026-07-22 정책 헌법 §7) |
| ③ | 예시 값 파일 ≥1 동봉 | 값 파일 폴더(`channels/` — 옛 이름 `presets/`, 실물 0개)에 실사용 검증된 것 1개+ |
| ④ | requires 선언 | 제디 도구·endpoint·config 의존 전부 명시 |
| ⑤ | 프롬프트 3종 패턴 준수 | 지시문이 수치 하드리밋 / 금지 리터럴 열거 / 예시쌍+셀프체크 중 ≥1 사용 (`prompt-authoring-protocol.md`) |
| ⑥ | 플러그인 이름 집합 일치 | `node scripts/check-plugin-consistency.js` → exit 0 (marketplace.json ↔ install.ps1 ↔ install.sh ↔ hook-doctor-v2.js). **Tier A**(활성화 집합 == ① − DEPRECATED) + **Tier B**(레거시 잔존 — DEPRECATED 이름이 안내 문구·설치 명령에 남는 드리프트 차단, `install.ps1:297` 실사례) 2단 검사. 2026-07-22 정책 헌법 §7 드리프트 봉합 |
| ⑦ | **스킬 이름 ASCII kebab-case** | 같은 스크립트의 **Tier C**. `name`은 `^[a-z][a-z0-9]*(-[a-z0-9]+)*$` — **한글·비-ASCII 금지**(명령 식별자에서 문자당 `-`로 소실 → 같은 글자수끼리 전부 겹쳐 엉뚱한 스킬이 실행됨. 2026-07-26 실사고). 플러그인 내 중복 0 + **플러그인 간 bare 이름 충돌 0**(접두 없는 `/name` 호출이 불확정해지므로). 🔴 **팩 접두 필수**(같은 스크립트 **Tier C-5**, 2026-08-02 "권장"→강제 승격): `jedi-core` → `jedi-*`, `zulgap-pack` → `zulgap-*`. **화이트리스트 방식이라 맵에 없는 팩은 면제** — `dev-pack`(`start-dev`·`wrapup-dev`)은 접미사형이 의도이므로 등록하지 않는다. 승격 이유: 직원은 `/jedi`·`/zulgap` 까지 쳤을 때 뜨는 **목록에서 스킬을 찾으므로**(team-guide.md), 접두사가 없으면 게이트·CI가 전부 초록인데 **아무도 못 찾는 스킬**이 된다(루트 `skills/`와 같은 클래스 — 존재하나 도달 0). 실사고: 2026-08-02 팀원 첫 PR #71이 `naver-rank-check`로 올라와 CI를 통과했고, 규칙이 여기 "권장"으로만 있어 자동으로는 아무도 못 잡았다. 🔴 **폴더명도 `name`과 동일한 ASCII 필수**(C-4) — 스킬 중복 판정(dedup)은 frontmatter 치환 **전** 폴더명 기반 ID에서 일어나 한글 폴더는 같은 글자수끼리 겹쳐 하나만 생존한다(2026-07-28 실사고: name만 ASCII로 바꾼 v2.5가 무효였음) |
| ⑧ | **`tier` 선언 + A급 리터럴 0** | 같은 스크립트의 **Tier D** (2026-07-30 신설). **D-1** `tier` 미선언 = FAIL / **D-2** `tier: shared` + A급 리터럴 = FAIL. `tenant-only`의 A급은 정보성 출력(FAIL 아님 — §1 단서 참조) |
| ⑨ | **형제 스킬 폴더명 하드코딩 0** | 같은 스크립트의 **Tier E** (2026-07-30 신설). SKILL.md 본문의 `../<구체적이름>/` 참조 = FAIL. **같은 스킬의 폴더명이 설치 형태마다 다르다**(팀팩 `jedi-image` / 개인 `이미지`) → 고정 이름을 박으면 **한쪽에서만 깨진다**(v2.20 실사고: 직원이 인물 사진을 올리면 실패, 게이트·CI 어디에도 안 걸려 사람이 발견할 때까지 잔존). 올바른 작성법 = `` `../<이미지 스킬 폴더>/` `` 플레이스홀더 + *"형제 폴더를 확인하고 실제 이름을 쓸 것"* 안내 — 플레이스홀더는 통과시킨다. 🔴 판정축은 *"참조한 파일이 존재하나"*가 **아니다** — 그건 형제 폴더를 정당하게 가리키는 문장까지 잡아 실측 오탐 1/12이 났다. *"폴더명이 박혔나"*는 구조 판정이라 오탐 0 |

| ⑩ | **스킬 상속 선언** | 같은 스크립트의 **Tier I** (2026-08-05 신설). 다른 스킬의 규칙을 따르면 frontmatter에 `extends: <정본>` + `parent-checksum: <부모 SKILL.md blob sha 앞 8자>`. **I-1** extends 대상이 실재해야 한다(오타·삭제·개명 차단) / **I-2** 본문이 `"…의 <단계\|규칙\|기준>를 따른다"`고 하는데 extends 미선언 = FAIL / **I-3** 부모가 마지막 확인 이후 바뀌었으면 FAIL. 🔴 신설 사유 = 2026-08-04 실사고: `zulgap-blog`에 이미지 기준을 넣었는데 그걸 따르는 `zulgap-gaon-blog`가 안 따라왔고 **우연히 발견됐다**. 관계가 본문 자연어로만 있어 기계가 몰랐던 것. 🔴 I-3의 판정값이 **날짜가 아니라 해시**인 이유 — 날짜는 지어낼 수 있어 "확인했다"는 자기 서명일 뿐이고 시간이 지나면 습관적으로 통과된다. 해시는 정확한 값을 넣어야 통과하고 그 값은 부모 파일을 봐야 얻는다. 사람 검증 = `git hash-object <부모 SKILL.md>` 앞 8자. 🔴 I-2 판정식을 넓히지 말 것 — 목적어 없는 넓은 판정은 실측 오탐이 났다(`jedi-skills` *"그대로 쓰면"*). 목적어 한정 + 자기참조 제외 2조건에서 23스킬 오탐 0 |

| ⑪ | **실행 이식성 · 번들 자산 라이선스** | 같은 스크립트의 **Tier F** (2026-08-03 신설). **F-1** `scripts/` 안 코드(`.js`·`.mjs`·`.cjs`·`.py`·`.ps1`·`.sh`)에 OS 종속 경로가 **단일 계열뿐**이면 FAIL — 다른 OS에서 그 줄에서 죽는다. 판정축은 *"윈도우 경로를 썼나"* 가 **아니라 "대안이 있나"** 다(후보 배열은 통과, 의도적이면 `@AI:ALLOW os-specific-path`). **F-2** 번들 폰트(`.ttf`·`.otf`·`.ttc`·`.woff`) 옆에 라이선스 사본(`OFL`/`LICENSE`/`NOTICE`) 없으면 FAIL — 레포가 public이라 **커밋이 곧 재배포**다. 🔴 신설 사유 = A~E가 `SKILL.md`만 봐서, 팀원 PC에서 실제로 죽는 `scripts/` 코드 3건이 CI·`node --check`·문서 리뷰를 **전부 통과한 뒤** e2e에서야 드러났다. 상세는 §1 「실행 이식성」 |
| ⑫ | **되돌릴 수 없는 외부 행위 호출 0** | 같은 스크립트의 **Tier G** (2026-08-05 신설). **G-1** 세금계산서 발행류(국세청 도달 = 제도상 취소 불가)를 스킬이 **호출 형태**(`도구명(`)로 지시하면 FAIL — 그 4종은 백엔드에서 MCP·파사드·비대화 경로를 전부 봉인하고 **텔레그램 인라인버튼(사람 클릭)** 하나만 남겼는데, 스킬이 부르라고 적으면 봉인을 문서가 우회한다. **G-2** 그 경계를 다루는 스킬은 *"발행하지 않는다"* 문구를 본문에 유지해야 한다(다음 사람이 지우면 경계를 모른 채 발행 절차를 덧붙인다). 🔴 산문 언급은 허용 — 호출 형태만 본다. *"이 스킬은 발행하지 않는다"* 고 설명하는 정직한 문서가 스스로 FAIL 하면 안 되기 때문이다 |
| ⑬ | **설치 stub 신선도** | 같은 스크립트의 **Tier H** (2026-08-05 신설). **H-1** `team-CLAUDE.md`·`team-CLAUDE-en.md` 에 「N시간마다/뒤/후」 같은 **주기·간격 숫자** 0건. 🔴 그 두 파일은 설치 때 팀원 PC로 **한 번 복사되고 그 뒤 갱신되지 않는다**(원격 fetch 대상은 `team-guide.md` 뿐). 그래서 구현 세부 숫자를 적으면 그 주기가 바뀌는 순간 **기존 직원 PC에 고칠 방법이 없는 거짓말**로 영구히 남는다. 정확한 값이 필요하면 `team-guide.md`(매 세션 원격 최신본)에 쓰고, stub 에는 *"자동입니다"* 처럼 값이 바뀌어도 안 낡는 표현만 남긴다 |
| ⑭ | **게이트↔문서 커버리지** | 같은 스크립트의 **Tier J** (2026-08-05 신설). **J-0** Tier 목록 SSOT(`TIERS` 상수) == 실제 출력 라벨 == 파일 상단 주석 목록 **3자 일치**(어느 하나만 고치면 FAIL — 손유지 장부가 되지 않게 하는 장치) / **J-1** 이 §4 정본이 전 Tier를 실었나 / **J-2** 「만드는 쪽」(`zulgap-make-skill`) ∪ 「검토하는 쪽」(`zulgap-pr-review`) 이 전 Tier를 실었나. 🔴 신설 사유 = **Tier F·G·H·I 가 신설됐는데 그것을 설명하는 문서 3벌이 안 따라왔다**(실측: 정본 B·G·H 누락 / 만드는 쪽 A·B·F·G·H 누락 / 검토하는 쪽 F·G·H·I 누락 — 셋이 서로 다르게 빠짐). 스크립트 상단 주석이 *"신설 시 이 목록 + workflow + spec §4를 함께 갱신"* 이라고 **이미 적어 뒀는데도** 벌어졌다. 🔴 판정에 `\b` 금지 — 문서가 한국어라 `Tier C·D·E`(중점)·`Tier H가`(조사)를 미탐한다(2026-08-05 실측). 🔴 재는 것은 **«완전 누락»뿐이고 설명 품질이 아니다** — 품질까지 재면 오탐이 늘어 다음 사람이 게이트를 우회한다(E·F·G가 전부 구조 판정으로 수렴한 이유) |
| ⑮ | **유출 패턴** | 같은 스크립트의 **Tier K** (2026-08-16 신설). 계좌번호(6-2-6)·사업자번호(3-2-5)·텔레그램 chat_id·사내 UNC 경로·개인 PC 절대경로를 **「이름」이 아니라 「모양」으로** 잡는다. 🔴 신설 사유 = **하루에 같은 클래스가 세 번** 났고(#205 공용 팩 → #208 옆 폴더 → #214 형제 폴더+스킬 본문 6곳) 샌 것은 전부 이름이 아니었다 — D-3(고객사 **이름** 목록)로는 **하나도 못 잡았다.** 모양으로 잡으면 목록에 없는 새 고객사·새 계정도 걸린다. 🔴 **baseline 에 값을 적지 않는다**(`scripts/_leak-baseline.json` = 파일·패턴이름·건수만) — 동결 목록에 실값을 적으면 그 목록이 또 유출이 된다(#205 가 게이트용 고객 명부를 만들어 공개한 것과 같은 실패). 🔴 오탐을 줄이려 **UNC 는 문서(.md)만** 보고(코드의 `\bTODO\` 정규식이 같은 모양 — 실측 15건 중 6건이 오탐) **예시 번호**(`123-45-67890` 순차·`000-00-00000` 반복)는 통과시킨다 — 꺼진 게이트는 없는 게이트보다 나쁘다 |

이 표가 그대로 **미래 마켓플레이스의 등록 심사 기준**이다.

> 🔴 **심사는 사람이 아니라 CI가 한다** (2026-07-30 실측 근거).
> 게이트가 검사하는 규칙 = **12/12 = 100% 준수** / 사람 눈으로만 보는 규칙 = **8%**.
> 같은 repo·같은 사람·같은 문서인데 갈린 유일한 차이가 **검사 코드의 유무**였다.
> 그리고 이 체커는 2026-07-22부터 있었으나 **CI에 안 걸려 있어** PR #53 이후 자기 자신이 6개 PR 동안 적색 방치됐다.
> → **게이트를 만드는 것보다 자동으로 돌게 하는 것이 결정적이다.** 배선: `.github/workflows/plugin-consistency.yml`

## §5 등급 분류

- `tier: shared` — core 팩 배치 가능, 이 프로토콜 전체 준수
- `tier: tenant-only` — **전용 팩(`<tenant>-pack/`)에만 배치**. core 팩 배치 금지. 본체에 회사 문맥 하드코딩 허용(어차피 그 회사만 씀)
- 판정 기준 1줄: "본체 알고리즘을 다른 회사가 그대로 쓸 수 있나?" YES → shared / NO → tenant-only
- tenant-only → shared 승격 = 본체에서 테넌트 값을 preset_slots로 빼내는 리팩터링 + §4 심사 통과

### 🔴 미선언 = `shared` 간주 (fail-closed, 2026-07-30)

`tier`를 안 적으면 **"전용이라 §1~§4 면제"인지 "공용인데 미준수"인지 구분이 불가능**하다 — §0의 면제 판정 자체가 성립하지 않는다.
그래서 **미선언은 안전한 쪽(`shared`)으로 간주**하고 Tier D가 FAIL을 낸다. **전용 스킬은 한 줄 적어야 면제된다.**
> 실측: 2026-07-30 이전 12개 스킬 중 `tier` 선언은 **1개(8%)**뿐이었다. 나머지 11개는 면제 대상인지 아닌지를 파일에서 증명할 수 없었다.

### ✅ `tenant-only`의 전제 — 조건부 활성화 이행 (2026-08-03)

`tenant-only`는 **그 회사에만 활성화**되는 것이 전제다. 그래야 §1이 A급을 허용하는 근거가 선다.
2026-07-30 시점엔 `install.sh`/`install.ps1`이 `zulgap-pack`을 무조건 활성화해 미이행이었고, 2026-08-03에 조건부로 전환했다.

**판정축** = `GET /mcp/ext/teampack-config` 의 `packs` 배열 (토큰 tenant 기준 — §1이 처방한 통로. 줄갭 카드 = `['core','zulgap','dev']`, Migration 437).

| packs 판정 | zulgap-pack |
|---|---|
| `'zulgap'` 포함 | 활성 + 설치 |
| 확실히 미포함 (고객사 토큰) | **명시적 false** + 설치 제외 |
| 판정 불가 (토큰 없음·네트워크 실패·404 카드 미등록·curl 부재) | **현상 유지(활성)** — confident 원칙 |

🔴 **confident 원칙** (dev-pack role 분기와 동일 클래스): 확실할 때만 끈다. 404를 exclude로 접으면 줄갭 카드 행이 실수로 지워진 날 전 팀원이 스킬을 잃는다('스킬 0개' 사고 클래스). 고객사는 온보딩 순서가 **카드 등록 → 토큰 발급**이라, 토큰이 있으면 카드도 있다 — 그때만 confident 판정이 선다.

**3장부 동기** (한쪽만 고치면 hook-doctor가 되돌린다): `install.sh` §2.6 + merge-settings / `install.ps1` §4.6 + §6 / `hooks/hook-doctor-v2.js` `tenantPacksWithConfidence()`(24h 캐시). hook-doctor가 매 세션 자가치유하므로 **설치기만 고치는 변경은 무효**다.

⚠️ 남은 한계: 이 레포 자체가 public이라 **GitHub에서 파일을 직접 열면** 전용 스킬 본문은 여전히 보인다. 활성화 게이트는 "고객사 PC에 스킬로 내려가 실행되는 것"을 막는 층이고, 열람 차단은 레포 분리(전용 팩 private repo) 몫 — 고객사 확장 시점에 검토.

## 인증 이력

| 스킬 | tier | 인증일 | 비고 |
|---|---|---|---|
| ~~썸네일~~ → `jedi-thumbnail` | shared | 2026-07-14 → **2026-07-30 재인증** | 1호 인증. ⚠️ **인증이 시간에 따라 조용히 실효됐던 사례** — 인증 시점엔 리터럴 0이었으나 이후 관리자 PC 절대경로·노션 DB ID가 본체에 유입됐고 `presets/`→`channels/` 개명으로 §4③ 근거 경로도 어긋났는데 **재검사 주체가 없어 아무도 몰랐다**. 2026-07-30에 A급 제거 + Tier D 자동 검사 배선으로 해소 |

> 🔴 **인증은 시점의 상태일 뿐 지속 보증이 아니다.** 위 사례가 그 증거다 — 그래서 §4⑧(Tier D)을 CI에 걸어 **매 PR마다 재검사**되게 했다. 인증 표는 이력이고, **판정 SSOT는 CI**다.

## 관련

- jedi-forge SKILL.md Phase 0-D (5) "팀팩/Claude Code 배포 스킬" 케이스 + Phase 2 체크리스트 게이트
- 백엔드 런타임 스킬 규격(층 다름): `~/.claude/skills/jedi-forge/references/skill-spec.md`
- 팀팩 거버넌스: main = PR 경유 + AI 셀프머지 금지 (push 권한 = 전 직원 PC 코드 실행 권한)
