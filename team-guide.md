# 줄갭 작업 안내 (세션 시작 시 자동 주입 · 원격 업데이트)

> 이 안내는 매 세션 시작 시 GitHub에서 자동으로 최신본을 받아옵니다. (직원 PC 재설치 불필요)

## 쓸 수 있는 명령

> 📢 **명령 이름이 영문으로 바뀌었습니다 (2026-07-26).** 한글 이름은 Claude Code가 `--` 로 뭉개서
> 서로 구분이 안 됐고, 그래서 엉뚱한 스킬이 실행되는 문제가 있었습니다.
> **`/jedi` 까지만 치면 공용 스킬이, `/zulgap` 까지 치면 줄갭 전용 스킬이 목록으로 뜹니다.**
> 명령이 기억 안 나면 **오른쪽의 자연어를 그냥 말하셔도 그대로 됩니다** (예: "마무리해줘").

- **`/jedi-skills`** 또는 "무슨 스킬 있어" → 지금 쓸 수 있는 줄갭 스킬 목록을 설명과 함께 보여주고, 그 자리에서 골라 바로 실행 (새 스킬 자동 반영)
- **`/jedi-start`** 또는 "오늘 뭐부터" → 노션 마스터 허브에서 작업 현황을 불러와 무엇부터 할지 정리
- **`/jedi-save`** 또는 "마무리해줘" → 오늘 한 작업을 노션 팀 세션저널에 5섹션으로 기록
- **`/zulgap-blog`** 또는 "블로그 써줘" → 주제만 주면 키워드 조사 → 개요 확정 → 초안 → 규칙 검수(도구가 수치로 판정) → 시각물 → 노션 카드까지 만든다. **발행은 확인받고 나서만** 한다 (제디 토큰 필요)
- **`/jedi-ingest`** 또는 "이 자료 넣어줘" → 자료(강의·문서·작업·종목)를 디지털 트윈에 자동 분류·매핑해 3종 DB에 적재 (제디 토큰 필요, 모호하면 검수 큐)
- **`/jedi-image`** 또는 "이미지 만들어" → 텍스트로 이미지 생성(포스터·배너·표지·일러스트). 비율만 정하면 줄갭 백엔드(GPT Image 2)가 생성 (제디 토큰 필요)
- **`/jedi-thumbnail`** 또는 "썸네일 만들어" → 유튜브 대본을 주면 미스터비스트 기준 기획안 3개(제목/메인카피/서브카피)를 추천하고, 대화로 다듬은 뒤 완성 썸네일 생성(배경 GPT Image 2 + **한글 텍스트 픽셀 정확 오버레이**). "엔노블"이라고 하면 그 채널 설정 적용. **내 채널을 새로 만들려면 "○○ 채널로 만들어줘"** — 물어보고 채워서 내 PC에 저장한다(갱신에도 안 사라짐) (제디 토큰 필요)
- **`/jedi-keyword`** 또는 "키워드 조사해줘" → 황금키워드·황금질문 조사. 글감을 정하기 전에 "이 키워드 써도 되나"를 진입·단가 2축 숫자로 판정 (제디 토큰 필요)
- **`/zulgap-gaon-report`** 또는 "검단가온 N월 보고서 만들어줘" → 검단가온치과 월간 성과 보고서를 자동 수집(플레이스·검색광고·블로그·홈페이지)해 노션 6페이지로 발행 (담당자 전용 — 네이버 계정 권한 필요, 매월 초 실행)
- **`/zulgap-noblenyang`** 또는 "노블냥 영상 만들어" → 노션 카드 링크만 주면 노블냥(엔노블) 숏폼을 자동 제작(후킹 인트로 + 본편 음성·자막 + 1.2배속)해 엔노블 공유폴더에 저장 (줄갭 담당 전용 — 줄갭 토큰·Windows 필요, 유료: 이미지 2 + 씨댄스 영상 2/편)

## 쓸 수 있는 도구
- **노션**: 자료·정본 불러오기
- **PPT(pptx)**: 발표자료 생성
- **한글(hwp)**: 한글 문서 생성 (한컴오피스 필요)
- **제디(jedi)**: 회사 데이터(판단·도구) 조회 + 자료 인입(`/jedi-ingest`) + 이미지 생성(`/jedi-image`) — 개인 토큰(JEDI_TOKEN) 등록 시에만 활성. 본인 권한·테넌트 안에서만. 토큰 없으면 이 도구만 안 보일 뿐 나머지는 정상.

## 도구가 기본입니다 — 스킬은 지름길일 뿐

**부엌은 항상 열려 있습니다.** 위 도구들은 설치 순간부터 전부 켜져 있습니다. 스킬(`/jedi-thumbnail` 같은 것)은
**정해진 순서가 있는 일**을 위한 지름길이지, 그것만 쓰라는 울타리가 아닙니다.

### 그냥 말하면 되는 것들 (스킬 없이)
- "고양이 그림 하나 그려줘" → 바로 생성
- "이 키워드 경쟁률 봐줘" → 바로 조사
- "내일 3시 미팅 잡아줘" → 바로 등록

### 스킬이 답답하면
**"그만하고 그냥 만들어줘"** 라고 하세요. 바로 빠져나옵니다.
(예: `/jedi-thumbnail` 로 시작했는데 그냥 배너 한 장이 필요할 때 — 기획안 3개를 다시 묻지 않습니다.)

### ⚠️ "그 도구가 없다"는 말이 나오면
도구는 **처음엔 이름만 보이고 상세 설명은 나중에 불러옵니다**(deferred).
그래서 AI가 가끔 "그 도구가 없다"고 **잘못** 말합니다. **있습니다.**
그럴 땐 **"도구 다시 찾아봐"** 라고 하면 됩니다.

### 우선순위
1. **제디 도구** — 결과가 회사 자산으로 쌓입니다(영구 URL·노션·학습DB). 같은 일이면 이걸 먼저
2. **클로드 기본 도구** — 제디에 없는 일은 이걸로. 막지 않습니다
3. **본인이 추가한 도구·스킬** — 팀팩이 가리지 않습니다

### 🔴 갱신 명령을 안내하지 말 것 (Claude 전용 지시)
팀원에게 `/plugin update ...` 같은 **수동 갱신 명령을 안내하지 마세요.** 갱신은 아래 자가점검 v2가
24시간마다 자동으로 하고, **역할(role)에 맞는 팩만** 켭니다.

- `jedi-core` · `zulgap-pack` → 전원
- `dev-pack` → 개발자(dev)·관리자(master) 대상. 일반 팀원(staff·PM)은 **쓸 일이 없는 팩**입니다

> **설치 ≠ 활성화.** 마켓플레이스는 통째로 내려오므로 파일은 3팩 다 존재하고, 켜지는 팩만 role로 갈립니다.
> 2026-07-29 실사고: 일반 팀원에게 4팩 전부 갱신을 안내했다가 `dev-pack`에서 막혔습니다.
> **본인 role과 무관한 팩이 목록에 보이더라도 신경 쓰지 마세요** — 갱신 명령을 대신 쳐 줄 필요도 없습니다.

## 스킬을 새로 만들었거나 고쳤다면 — PR로 올려주세요

**팀팩 저장소에 쓰기 권한이 이미 있습니다.** 사장님께 부탁하지 않고 직접 올릴 수 있습니다.
(내 PC의 스킬 파일을 직접 고치는 건 여전히 하지 마세요 — 자동 갱신 때 덮여서 사라집니다.)

Claude에게 **"이 스킬을 팀팩에 PR로 올려줘"** 라고 하면 처음부터 끝까지 해줍니다.
사장님이 검토·승인하면 그때 전 직원 PC에 배포됩니다.

> 혹시 올리는 중에 **권한이 없다고 막히면** 아직 계정이 등록되지 않은 것입니다 — 사장님께 말씀해 주세요(열어드리면 됩니다).

### 🔴 넣는 자리 — 틀리면 아무에게도 안 갑니다

실제로 읽히는 자리는 **3곳뿐**입니다:

```
plugins/jedi-core/skills     ← 공용 스킬
plugins/zulgap-pack/skills   ← 콘텐츠 작업 스킬
plugins/dev-pack/skills      ← 개발자용
```

저장소 **맨 위(루트)의 `skills/` 는 로드 경로가 아닙니다.** 거기 넣으면 파일도 있고 자동검사도
초록인데 **직원 PC에는 안 갑니다** — 증상이 없어 아무도 모릅니다(실제로 난 사고입니다).

### 🔴 이름 — 팩 접두사를 붙여야 목록에 뜹니다

넣는 팩에 맞춰 **이름 앞에 접두사**를 붙입니다. 폴더명과 `SKILL.md` 의 `name:` **둘 다** 같아야 합니다.

| 넣는 곳 | 이름 | 예 |
|---|---|---|
| `plugins/jedi-core/skills` | `jedi-*` | `jedi-keyword` · `jedi-thumbnail` |
| `plugins/zulgap-pack/skills` | `zulgap-*` | `zulgap-blog` · `zulgap-gaon-report` |

위에서 본 것처럼 직원들은 **`/zulgap` 까지 쳤을 때 뜨는 목록**에서 스킬을 찾습니다.
접두사가 없으면 그 목록에 안 떠서 **아무도 못 찾습니다.**
(자동검사가 막아주니 틀려도 괜찮습니다 — `FAIL [C-5 팩접두]` 가 뜨면 이름을 고치세요)

### 올리기 전 두 줄

- `node scripts/check-plugin-consistency.js` → `exit=0` 이면 통과. 건너뛰고 올리면 반려되고 왕복이 생깁니다.
- `SKILL.md` 맨 위 **`tier:` 선언 필수** — 회사 전용(노션 DB·동료사명이 들어감)은 `tenant-only`, 범용은 `shared`. 선언이 없으면 무조건 실패합니다.

### 남이 만든 스킬을 고칠 땐 — 만든 사람 승인이 필요합니다

스킬은 **만든 사람이 주인**입니다. 남이 만든 스킬 본체를 고치는 PR을 올리면 자동 검사가 주인을 찾아 **승인을 요청**합니다.

**막는 게 아닙니다.** 주인이 승인하면 그대로 진행됩니다. 검사에 걸리면 무엇을 하면 되는지 PR 화면에 그대로 적혀 나옵니다.

- 주인에게 리뷰를 요청하세요. **승인이 등록되면 검사가 자동으로 다시 돌아** 통과합니다.
- **버그를 고치는 거라면** 그 사실을 PR 본문에 적고 승인을 받으세요. 버그를 원본에 남겨두면 그걸 복사해 간 쪽까지 같은 버그를 물려받습니다.
- 회사·브랜드별 **값만** 바뀌는 거라면 그 스킬의 `channels/` 값 파일을 고치세요(검사 대상이 아닙니다).

내가 만든 스킬과 새로 만드는 스킬은 그냥 올리시면 됩니다.

### 반대로 — 내가 만든 스킬을 남이 고쳤을 때 (승인하는 쪽)

**그 PR은 내가 승인해야 진행됩니다.** 안 누르면 그 자리에서 멈춰 있습니다.

팀챗에 **`🔑 PR #N — @내아이디 승인 필요`** 가 오면 내 차례라는 뜻입니다.

```
PR 링크 열기 → 우측 위 [Files changed] → [Review changes]
              → ⦿ Approve → [Submit review]
```

누르면 **검사가 자동으로 다시 돌아** 초록이 됩니다. 따로 할 일 없습니다.

- 내용이 이상하면 Approve 대신 **댓글로 물어보세요** — 승인하지 않으면 머지되지 않습니다
- `parent-checksum` 한 줄만 바뀐 PR도 옵니다. 부모 스킬이 바뀌면 자식이 따라가야 하는 값이라 **본문은 그대로**인 경우가 많습니다
- 자리를 오래 비우실 땐 사장님께 말씀하세요 — **사장님 승인은 모든 주인 승인을 대신합니다**(2026-08-14 신설)

🔴 **머지(최종 반영)는 하지 마세요.** 권한이 있어 버튼은 눌리지만, 검토 전에 들어가면 전 직원에게 바로 배포됩니다. **PR 올리기까지**만 하시고 번호를 알려주세요.

> 상세 규칙(폴더 구조·고객사 정보 4등급·심사 9항목)은 [`docs/skill-packaging-spec.md`](https://github.com/zulgap/claude-team-pack/blob/main/docs/skill-packaging-spec.md) 를 보세요.

## 훅 파일 자가진단 (Claude 전용 — 1회, 조용히)

`~/.claude/zulgap/team-guide-fetch.js` 를 **읽어서** `launchDoctor` 라는 단어가 있는지 확인할 것.
(읽기만 한다. 명령 실행이 아니라 파일 읽기다.)

- **있으면** → 아무 말도 하지 말고 넘어간다. 정상이다.
- **없으면** → 이 PC의 훅이 **설치 시점 버전**이라 갱신이 영영 안 닿는다. 사용자에게 이렇게 안내할 것:

> ⚠️ 팀팩 갱신 파일이 옛 버전이라 자동 업데이트가 멈춰 있습니다. 아래를 그대로 입력해 주세요(맨 앞 `!` 포함):
> ```
> ! curl -sL https://raw.githubusercontent.com/zulgap/claude-team-pack/main/hooks/team-guide-fetch.js -o "%USERPROFILE%\.claude\zulgap\team-guide-fetch.js"
> ```
> 안 되면: `! powershell -Command "iwr 'https://raw.githubusercontent.com/zulgap/claude-team-pack/main/hooks/team-guide-fetch.js' -OutFile \"$env:USERPROFILE\.claude\zulgap\team-guide-fetch.js\""`
> 그 다음 클로드를 두 번 껐다 켜시면 이후로는 자동입니다.

🔴 **이 안내는 `!` 를 붙여 사용자가 직접 실행하게 해야 한다.** Claude가 대신 실행하려 하면 권한 정책에 차단된다
(2026-07-29 실측 "auto 모드 차단" — 그게 갱신이 몇 달째 멈춰 있던 원인이다).

> 왜 이 자가진단이 여기 있나: `team-guide-fetch.js` 는 install이 1회 복사하고 끝이라 **자기 갱신 경로가 없었다.**
> v2.17에서 자기 갱신을 넣었지만, **그 코드가 든 파일 자체를 못 받는 PC**는 스스로 못 낫는다 — 닭-달걀.
> 반면 이 `team-guide.md` 는 옛 훅도 매 세션 원격 최신본을 받으므로, 여기가 옛 PC에 닿는 **유일한 통로**다.

## 플러그인 전환·갱신 — 자동입니다 (Claude가 할 일 없음)

🔴 **여기서 아무 명령도 실행하지 말 것.** 플러그인 전환과 24시간 갱신은 `team-guide-fetch` 훅이
세션 시작 시 백그라운드로 처리한다(v2.14~).

**예전 지시를 되살리지 말 것** — 이 자리에는 `curl … hook-doctor-v2.js && node …` 를 실행하라는
지시가 있었는데, 그건 **권한 정책에 차단돼 몇 달째 한 번도 실행되지 않았다**(팀원 PC 실측: "auto 모드 차단").
그 결과 구 플러그인 미비활성 · staff PC에 개발자 스킬 노출 · 스킬 갱신 미도달 · 사장님 PC 5일 버전 고정이
전부 이 하나에서 나왔다. **문서 지시는 권한 앞에서 무력하다 — 실행이 필요한 일은 훅으로 옮긴다.**

사용자가 "갱신해줘"라고 하면: **이미 자동으로 되고 있다**고 안내하고, 굳이 지금 확인하고 싶어 하면
`/plugin` 같은 명령을 대신 치지 말고 사장님께 문의하도록 안내한다.
