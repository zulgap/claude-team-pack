# 스킬 소유권 — "만든 사람이 정본 소유자" (2026-08-04 사장님 확정)
#
# 이 파일이 하는 일: 아래 경로가 PR에 포함되면 GitHub이 그 소유자를 리뷰어로
# 자동 지정하고, 브랜치 보호의 "Require review from Code Owners"가 켜져 있으면
# 소유자 승인 없이는 머지되지 않는다.
#
# 판정 근거는 이 파일이 아니라 git이다:
#   git log --follow --diff-filter=A --format='%an' -- <경로>/SKILL.md | tail -1
# 이 파일은 그 결과를 GitHub이 읽을 수 있는 형식으로 옮겨 적은 것이므로,
# git author와 어긋나면 git이 정본이다. (frontmatter `author` 필드를 만들지
# 않은 것과 같은 이유 — 손으로 쓰는 장부는 두 번째 장부가 되어 드리프트한다)
#
# 정책 정본: memory/project_skill_fork_model_design.md § 소유권 정책(2026-08-04)
#   ① 남의 본체(SKILL.md) 수정 대신 channels/ 값 파일이 1순위
#   ② 버그 수정은 예외 (소유자 승인 시)
#   ③ 사장님은 팀원 스킬도 고칠 수 있다 — 단 PROVENANCE.md에 사유 1줄
#
# ⚠️ 여기 지정된 계정은 레포에 write 이상 권한이 있어야 리뷰어로 지정된다.
#    (2026-08-04 실측: naraeng2·jy8250-sketch 둘 다 write ✅)

# ─── 기본 소유자 ────────────────────────────────────────────────
# 아래에서 다시 지정되지 않은 모든 경로. 마지막 매칭 규칙이 이기므로
# 이 줄은 반드시 맨 위에 있어야 하고, 팀원 소유 경로가 이를 덮어쓴다.
*                                                       @zulgap

# ─── 팀원 소유 스킬 ─────────────────────────────────────────────
# 2026-08-04 git 실측 21개 중 5개. 나머지 16개는 위 기본 규칙(@zulgap).

/plugins/zulgap-pack/skills/zulgap-cardnews/            @jy8250-sketch
/plugins/zulgap-pack/skills/zulgap-ennoble-thumbnail/   @jy8250-sketch
/plugins/zulgap-pack/skills/zulgap-rank-check/          @jy8250-sketch
/plugins/zulgap-pack/skills/zulgap-scheduling/          @jy8250-sketch

/plugins/zulgap-pack/skills/zulgap-gaon-blog/           @naraeng2
